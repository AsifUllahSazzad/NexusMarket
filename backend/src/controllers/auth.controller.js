export const postMerchant = (req, res) => {
  const body = req.body;
  console.log(body);

  res.send();
};

export const postCustomer = (req, res) => {
  const body = req.body;
  console.log(body);

  res.send();
};
